// The kanban column colour picker's swatch names, in picker order. Each swatch's accessible name +
// title is its colour name (a colour, not a token name, reads better to a keyboard/screen-reader
// user). Shared by KanbanView (which renders the picker) and its stories (which assert on it).
export const PALETTE_NAMES = ['rose', 'violet', 'blue', 'teal', 'green']

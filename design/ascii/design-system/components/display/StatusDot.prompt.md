Status as a colored dot plus its word — Bases tables, kanban headers, service lists.

```jsx
<StatusText status="reading" />
<StatusDot color="var(--green)" />
```

Colors are categorical (`--teal/--blue/--green/--rose`), not semantic.

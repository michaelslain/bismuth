Note-editing surface — frontmatter block + ruled prose. The editor column is capped at 620px.

```jsx
<MarkdownField frontmatter={{ icon: "(*)", created: "2029-09-15T07:12", brain: 2 }}>
  I walked the long way home and let the day unspool behind me.
</MarkdownField>
```

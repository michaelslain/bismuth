Dropdown — an input-styled trigger plus a PopoverList. The caret is `v` / `^`, typed.

```jsx
<Select value={theme} onChange={setTheme}
        options={[{id:"ink",label:"ink"},{id:"paper",label:"paper"}]} />
```

Glyph-only button for chrome — window controls, tab-rail entries, close affordances.

```jsx
<IconButton glyph="⁘" state="selected" title="graph" />
<IconButton glyph="x" title="close tab" />
```

The glyph is always a typed character; this system ships no SVG icon set.

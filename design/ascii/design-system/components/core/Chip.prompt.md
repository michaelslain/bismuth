Selectable pill — tag filters, search toggles, export options.

```jsx
<Chip selected tone="teal">#lichen 4</Chip>
<Chip>#walking 18</Chip>
```

Tone is categorical: use it when the chip *is* a category, not for emphasis.

Borderless label button — inline affordances inside panels and headers.

```jsx
<TextButton bracket onClick={openGraph}>open</TextButton>
```

Use when a bordered Button would add a second box inside an already-bordered panel.
